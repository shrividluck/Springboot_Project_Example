package com.shri.mtts.service;
import com.shri.mtts.entity.Theater;

public interface MovieService {
	Theater addMovie(Theater movie);

	void updateMovie(Theater movie);

	Theater getMovie(long Id);

	/**
	 * Search user by first or last name, partial searches also performed
	 * 
	 * @param firstName
	 * @param lastName
	 * @return Empty list is returned if none found
	 */
	Iterable<Theater> getMovies(String name);

	//boolean isATMPinValid(long userId, String pin);

}
